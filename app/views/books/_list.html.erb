<% if @books.count == 0 %>

  <h1>There are no books currently in this borrowed </h1>

  <% if borrowed.user_id == current_user.id %> %>  

    <div class="row">

      <% @books.each do |book| %>

        <div class="col-md-3">
          
          <a href="/books/<%= book.id %>">
            <%= image_tag book.book_img.url(:book_index), class: "book" %>
          </a>

        </div>  
  <% else %>
    
  <% end %>

    </div>
  
<% end %> 

